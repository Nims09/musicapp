<%= form_for @stock, :html => { :class => 'form-horizontal' } do |f|%>
  <% if @stock.errors.any? %>
	  <div id="error_explanation">
		    <h2><%= pluralize(@stock.errors.count, "error") %> prohibited
		      this stock from being saved:
				</h2>
	    <ul>
		    <% @stock.errors.full_messages.each do |message| %>
		      <li><%= message %></li>
		    <% end %>
	    </ul>
	  </div>
  <% end %>
	<p>
		<%= f.label :name, :class=>".control-group" %>
		<%= f.text_field :name, :class=>".controls"%>
	</p>

	<p>
		<%= f.label :hashtag, :class=>".control-group" %>
		<%= f.text_field :hashtag, :class=>".controls" %>
	</p>
	<%= f.hidden_field :user_id%>

  <%= f.submit 'submit', :class => 'btn btn-primary'%>
<% end %>
